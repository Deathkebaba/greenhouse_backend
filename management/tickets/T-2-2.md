---
id: T-2-2
plan_id: P-2
title: "Push diary tag filtering into SQL"
goal: "Move diary tag filtering from post-load Rust filtering into the SQL query layer inside data_storage_service without changing external behavior."
scope:
  - "Refactor the diary date-range query in data_storage_service so requested tags are applied in SQL instead of filtering hydrated entries in Rust memory."
  - "Preserve current tag semantics: case-insensitive matching, trimmed and deduplicated requested filters, original-case preservation in stored and returned tags, and both `Any` and `All` filter modes."
  - "Add focused validation for the new query shape so the behavior and the intended persistence-layer execution are both protected."
acceptance:
  - "Diary tag filtering for date-range queries executes in the database layer rather than after loading the full date-range result set into Rust memory."
  - "`Any` and `All` modes preserve the same externally visible matching behavior that P-1 introduced."
  - "Focused validation covers the SQL-backed filtering path and guards against regressions in semantics or accidental fallback to post-load filtering."
constraints:
  - "Keep diary filtering ownership inside data_storage_service and do not move domain logic into api/web or greenhouse_core."
  - "Do not change the public diary query contract unless a separate approved plan requires it."
  - "Favor a query shape that is performant and still readable enough to remain maintainable under growth."
history:
  - at: "2026-05-04T00:00:00Z"
    actor: "planner-agent"
    action: "created"
  - at: "2026-05-03T23:11:59Z"
    actor: "manager-agent"
    action: "claimed"
    summary: "Claimed for implementer work on branch ticket/T-2-2."
---