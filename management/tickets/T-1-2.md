---
id: T-1-2
plan_id: P-1
title: "Implement diary image and tag persistence"
goal: "Teach data_storage_service to store diary images, manage image lifecycle, and filter diary entries by tags."
scope:
  - "Implement data_storage_service diary persistence for multiple database-backed images per entry plus image metadata assembly."
  - "Add service routes for image upload, download, and single-image deletion tied to diary entries."
  - "Implement tag persistence and date-range diary filtering with both any-tag and all-tags modes using case-insensitive matching."
  - "Support diary edit flows that add images and remove individual images without breaking existing entry updates."
acceptance:
  - "data_storage_service persists uploaded image bytes in its owning database and returns metadata required by the shared response DTOs."
  - "Image download and deletion routes enforce real diary/image existence checks and return honest errors on invalid requests."
  - "Diary queries combine existing date-range behavior with tag filters and mode semantics for any-tag and all-tags matching."
  - "Focused validation covers the new data_storage_service diary behavior."
constraints:
  - "Keep business rules, storage invariants, and filtering semantics in data_storage_service."
  - "Do not introduce filesystem or external object storage for this ticket."
  - "Preserve backward-compatible behavior for diary entries that have no tags or images."
history:
  - at: "2026-05-03T21:27:31Z"
    actor: "planner-agent"
    action: "created"
  - at: "2026-05-03T21:50:31Z"
    actor: "manager-agent"
    action: "claimed"
    summary: "Claimed for implementer work on branch ticket/T-1-2."
  - at: "2026-05-03T21:56:13Z"
    actor: "implementer-agent"
    action: "implemented"
    summary: "Implemented data_storage_service diary tag persistence, image blob persistence and metadata assembly, date-range tag filtering, and image upload/download/delete routes while preserving diary update behavior for existing entries."
  - at: "2026-05-03T21:56:13Z"
    actor: "implementer-agent"
    action: "validated"
    summary: "`cargo check -p data_storage_service --tests` passed. `cargo test -p data_storage_service` was blocked in this Windows environment by missing PostgreSQL linker input libpq.lib, so the new focused route tests could not be executed end-to-end here."
  - at: "2026-05-03T21:57:00Z"
    actor: "manager-agent"
    action: "review_started"
    summary: "Moved T-1-2 to review after implementer completion on branch ticket/T-1-2."
  - at: "2026-05-03T23:58:38Z"
    actor: "reviewer-agent"
    action: "review_submitted"
    summary: "Changes requested: diary metadata reads now hydrate full image blobs on every fetch."
    verdict: changes_requested
    next_actor: implementer
    directive_compliance:
      status: compliant
      notes:
        - "The implementation keeps diary image/tag ownership and filtering semantics inside data_storage_service, which matches the service-boundary directives."
        - "The new entry-scoped image routes align with the shared greenhouse_core endpoint contract established in T-1-1."
      violations: []
    findings:
      critical: []
      major:
        - location: "services/data_storage_service/src/database/diary_models.rs:49, services/data_storage_service/src/database/diary_models.rs:422"
          risk: "Diary entry reads now load image_data for every related image even when the API only needs DiaryImageMetadataDto. That makes ordinary get-by-id and date-range diary queries scale with total stored blob size, creating avoidable memory and query-cost regressions as images accumulate."
          suggested_fix: "Split image metadata reads from blob reads. Keep a metadata-only row type or explicit Diesel select for diary_entry_image list/get paths, and reserve image_data loading for the dedicated download_image path. Add a focused regression test that exercises entry retrieval after uploading images and verifies the metadata path does not depend on blob hydration."
      minor: []
    missing_tests:
      - "The new service-level upload/download/delete and tag-filter tests were not executable in this Windows environment because PostgreSQL-linked test binaries still require libpq.lib, so the reviewed behavior could not be re-run end to end here."
      - "No focused regression test currently protects the diary metadata read path from accidentally selecting image_data on normal entry list/get queries."
    residual_risks:
      - "Until the metadata read path stops selecting blobs, diary retrieval cost will rise with stored image payload size even when clients only request entry metadata."
  - at: "2026-05-03T23:59:10Z"
    actor: "manager-agent"
    action: "rework_started"
    summary: "Restarted implementation for T-1-2 to split metadata reads from blob reads in normal diary fetch paths."
  - at: "2026-05-04T00:01:09Z"
    actor: "implementer-agent"
    action: "implemented"
    summary: "Split normal diary image metadata reads away from blob reads in data_storage_service by using a metadata-only image select for entry get and date-range queries while keeping image bytes on the dedicated download path."
  - at: "2026-05-04T00:01:09Z"
    actor: "implementer-agent"
    action: "validated"
    summary: "`cargo check -p data_storage_service --tests` passed after the metadata/blob query split. The focused regression test `cargo test -p data_storage_service diary_metadata_query_does_not_select_image_bytes --lib` remains blocked in this Windows environment because linking test binaries still fails on missing `libpq.lib`."
  - at: "2026-05-04T00:01:40Z"
    actor: "manager-agent"
    action: "review_started"
    summary: "Moved T-1-2 back to review after the metadata/blob query split on branch ticket/T-1-2."
  - at: "2026-05-03T22:02:55Z"
    actor: "reviewer-agent"
    action: "review_submitted"
    summary: "No findings"
    verdict: approved
    next_actor: manager
    directive_compliance:
      status: compliant
      notes:
        - "Normal diary get/list paths now hydrate image metadata through an explicit metadata-only select, keeping blob reads out of ordinary entry retrieval."
        - "Blob loading remains isolated to the dedicated image download path inside data_storage_service, preserving service ownership and the intended route contract."
      violations: []
    findings:
      critical: []
      major: []
      minor: []
    missing_tests: []
    residual_risks:
      - "The SQL-shape regression test for metadata-only reads exists, but the broader PostgreSQL-linked test binaries still could not be executed end to end in this Windows environment because libpq.lib is unavailable."
---
